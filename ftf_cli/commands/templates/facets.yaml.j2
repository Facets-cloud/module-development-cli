intent: {{ intent }}
flavor: {{ flavor }}
version: "1.0"
clouds: {{ clouds }}
description: {{ description }}
spec:
  title: {{ title }}
  description: {{ description }}
  type: object
outputs:
  default:
     type: "@outputs/{{ intent }}"
sample:
  kind: {{ intent }}
  flavor: {{ flavor }}
  version: "1.0"
  disabled: true
  spec:
    type: object
    properties:
